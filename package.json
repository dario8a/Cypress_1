{
  "name": "pre-entrega",
  "version": "1.0.0",
  "description": "",
  "main": "preentrega.cy.js",
  "scripts": {
    "test": "npx cypress open --e2e -b chrome",
    "test1": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.16.0",
    "cypress-xpath": "^2.0.1"
  }
}
